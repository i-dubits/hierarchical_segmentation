base_config: common.yaml

data:
  split_file: splits_deeplab_ft.json
  # Use dataset-provided train/val ids and carve a deterministic test split from train ids.
  id_based_test_ratio: 0.1
  image_size: [320, 320]

model:
  name: deeplabv3_mobilenet_v3_large
  pretrained: true
  pretrained_backbone: true
  freeze_backbone: false
  dropout: 0.1

training:
  epochs: 20
  batch_size: 16
  lr: 0.0001
  scheduler:
    name: cosine
    warmup_epochs: 2
    warmup_start_factor: 0.2
    eta_min: 0.000001
